{
  "$schema": "https://schema.railway.com",
  "builder": "RAILPACK",
  "build": {
    "buildCommand": "python -m pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python -m uvicorn api:app --host 0.0.0.0 --port $PORT"
  }
}
